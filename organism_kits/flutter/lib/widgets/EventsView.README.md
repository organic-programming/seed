# EventsView

Public API: `EventsView(controller: EventsController)`.

Customisation: inherits text and icon theme; event badges can be wrapped by app-level themes.

Sizing: scrolls vertically.

Embedding: use as the Events tab or as a compact timeline panel.
