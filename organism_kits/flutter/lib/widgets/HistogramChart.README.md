# HistogramChart

Public API: `HistogramChart(snapshot: HistogramSnapshot)`.

Customisation: bars inherit `ColorScheme.secondary`.

Sizing: parent provides a fixed height; 96x28 is the compact default.

Embedding: use in histogram rows or dashboards.
