# SparklineView

Public API: `SparklineView(values: List<double>)`.

Customisation: line colour inherits `ColorScheme.primary`.

Sizing: parent provides a fixed height; 72x24 is the compact default.

Embedding: use inside metric rows for sliding gauge history.
