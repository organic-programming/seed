# LogConsoleView

Public API: `LogConsoleView(controller: LogConsoleController)`.

Customisation: level badge colours follow Material colours and parent typography.

Sizing: flexes vertically; use at least 360px width for readable rows.

Embedding: usually the Logs tab inside `ObservabilityPanel`.
