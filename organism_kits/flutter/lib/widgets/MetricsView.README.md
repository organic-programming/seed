# MetricsView

Public API: `MetricsView(controller: MetricsController)`.

Customisation: sparklines and histograms inherit `ColorScheme.primary` and `ColorScheme.secondary`.

Sizing: scrolls vertically and adapts to narrow panels.

Embedding: bind to a kit-owned `MetricsController`.
