# CoaxSettingsView

Public API: `CoaxSettingsView(coaxManager: CoaxManager)`.

Customisation: inherits Material form, segmented button, text field, and dialog themes.

Sizing: dialog content is constrained to 720px and scrolls when needed.

Embedding: show from `showDialog` or place inside an app settings route.
