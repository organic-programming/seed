# CoaxControlsView

`CoaxControlsView(coaxManager:isShowingSettings:)` is the compact public COAX
control surface. It exposes the enable toggle, settings button, endpoint, state
badge, and status detail.

The widget has minimalist defaults and inherits host typography, tint, and
layout. Reference apps can preserve their existing look by wrapping it in their
own toolbar or overlay styling.

