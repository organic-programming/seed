# SparklineView

`SparklineView(values:)` draws a compact line chart for recent numeric samples.
It uses the current accent color and expects a fixed height from the parent.

Use it for gauge trends or other low-density telemetry histories. Empty or
single-sample inputs render as an intentionally blank sparkline.

