# HistogramChart

`HistogramChart(snapshot:)` renders a histogram snapshot as proportional bucket
bars with bucket help labels.

The chart uses the current accent color and should be placed in a container with
a stable height. The parent owns labels, legends, and surrounding metric text.

