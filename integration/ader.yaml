storage:
  reports: reports
  archives: archives
  artifacts: .artifacts
  temp_alias: .t
defaults:
  suite: seed
  source: committed
  lane: regression
  profile: quick
  ladder: [quick, unit, integration, full]
  archive_policy:
    quick: never
    unit: never
    integration: never
    full: never
    stress: auto
