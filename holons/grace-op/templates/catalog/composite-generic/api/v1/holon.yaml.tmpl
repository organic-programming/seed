schema: holon/v0
uuid: "{{ .UUID }}"
given_name: "{{ .GivenTitle }}"
family_name: "{{ .FamilyTitle }}"
motto: "{{ .HolonKind }} + {{ .HostUIKind }} holon composite."
composer: "op new"
status: draft
born: "{{ .Date }}"
description: "{{ .Slug }} scaffold generated from {{ .Template }}."
lang: "{{ .Lang }}"
kind: composite
build:
  runner: recipe
  defaults:
    target: macos
    mode: debug
  members:
    - id: holon
      path: holon
      type: holon
    - id: app
      path: app
      type: component
  targets:
    macos:
      steps:
        - assert_file:
            path: app/app.txt
    linux:
      steps:
        - assert_file:
            path: app/app.txt
    windows:
      steps:
        - assert_file:
            path: app/app.txt
artifacts:
  primary: app/app.txt
