# {{ .PascalSlug }} Holon

Replace this placeholder with the real business holon for `{{ .Slug }}`.

The scaffolded app holon already exposes the full COAX surface and keeps the
member id `holon` reserved for the business runtime you add next.
