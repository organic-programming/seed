schema: holon/v1
uuid: "{{.UUID}}"
given_name: "{{.GivenTitle}}"
family_name: "{{.FamilyTitle}}"
motto: "Python daemon scaffold."
composer: "grace-op scaffold"
status: "draft"
born: "{{.Date}}"
description: "Generated Python daemon."
lang: "python"
kind: "composite"
build:
  runner: "python"
requires:
  files: ["app/main.py"]
artifacts:
  primary: "app/main.py"
