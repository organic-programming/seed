# {{ .PascalSlug }} Holon

Replace this placeholder with the real business holon for `{{ .Slug }}`.

The scaffolded app member already exposes the full COAX surface and uses the
member id `holon` consistently. Wire your real runtime here, then update the
app-side discovery and connection logic to target the new artifact.
