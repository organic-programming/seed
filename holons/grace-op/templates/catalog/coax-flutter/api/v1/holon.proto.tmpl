syntax = "proto3";
package holons.v1;

import "holons/v1/coax.proto";
import "holons/v1/manifest.proto";

option (holons.v1.manifest) = {
  identity: {
    schema: "holon/v1"
    uuid: "{{ .UUID }}"
    given_name: "{{ .GivenTitle }}"
    family_name: "{{ .FamilyTitle }}"
    motto: "Flutter app holon scaffold with COAX support."
    composer: "op new"
    status: "draft"
    born: "{{ .Date }}"
    version: "0.1.0"
  }
  description: "{{ .Slug }} scaffold generated from {{ .Template }}."
  lang: "dart"
  kind: "composite"
  transport: "stdio"
  platforms: ["macos", "windows", "linux"]
  contract: {
    proto: "api/v1/holon.proto"
    service: "holons.v1.CoaxService"
    rpcs: [
      "ListMembers",
      "MemberStatus",
      "ConnectMember",
      "DisconnectMember",
      "Tell",
      "TurnOffCoax"
    ]
  }
  build: {
    runner: "recipe"
    defaults: {
      target: "macos"
      mode: "debug"
    }
    members: {
      id: "holon"
      path: "holon"
      type: "holon"
    }
    members: {
      id: "app"
      path: "app"
      type: "component"
    }
    targets: {
      key: "macos"
      value: {
        steps: {
          assert_file: {
            path: "app/lib/main.dart"
          }
        }
        steps: {
          assert_file: {
            path: "app/tool/package_desktop.dart"
          }
        }
      }
    }
    targets: {
      key: "linux"
      value: {
        steps: {
          assert_file: {
            path: "app/lib/main.dart"
          }
        }
      }
    }
    targets: {
      key: "windows"
      value: {
        steps: {
          assert_file: {
            path: "app/lib/main.dart"
          }
        }
      }
    }
  }
  artifacts: {
    primary: "app/lib/main.dart"
  }
};
