schema: holon/v0
given_name: gudule
family_name: Greeting-Qt-Kotlin
kind: composite
transport: stdio
platforms: [macos]
build:
  runner: recipe
  defaults:
    target: macos
    mode: debug
  members:
    - id: daemon
      path: ../../daemons/gudule-daemon-greeting-kotlin
      type: holon
    - id: app
      path: ../../hostui/gudule-greeting-hostui-qt
      type: holon
  targets:
    macos:
      steps:
      - build_member: daemon
      - build_member: app
      - copy:
          from: ../../hostui/gudule-greeting-hostui-qt/.op/build/bin/gudule-greeting-goqt
          to: build/gudule-greeting-goqt
      - copy:
          from: ../../daemons/gudule-daemon-greeting-kotlin/.op/build/bin/gudule-daemon-greeting-kotlin
          to: build/daemon/gudule-daemon-greeting-kotlin
      - assert_file:
          path: build/gudule-greeting-goqt
      - assert_file:
          path: build/daemon/gudule-daemon-greeting-kotlin
requires:
  commands: [java, cmake]
artifacts:
  primary: build/gudule-greeting-goqt
