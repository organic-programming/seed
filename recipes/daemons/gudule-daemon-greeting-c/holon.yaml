# ── Identity ──────────────────────────────────────────
schema: holon/v0
uuid: "a9b515e3-0b75-44f5-ab68-70c4d53e4947"
given_name: gudule
family_name: Greeting-Daemon-C
motto: Greets users in 56 languages — a C daemon recipe example.
composer: B. ALTER
clade: deterministic/pure
status: draft
born: "2026-03-13"

# ── Lineage ───────────────────────────────────────────
parents: []
reproduction: manual
generated_by: manual

# ── Description ───────────────────────────────────────
description: |
  A C daemon that greets users in 56 languages. Designed to be
  embedded in a HostUI but also usable standalone. Demonstrates
  the C implementation of the canonical greeting daemon using the
  Connect protocol over HTTP via the c-holons SDK.

# ── Operational ───────────────────────────────────────
kind: native
build:
  runner: cmake
requires:
  commands: [cmake, make]
  files: [CMakeLists.txt]
artifacts:
  binary: gudule-daemon-greeting-c
