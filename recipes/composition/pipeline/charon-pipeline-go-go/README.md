# Composition-Pipeline-Go-Go

Stages the shared Go workers under `build/holons/` and runs a `go` orchestrator from a staged launcher.

## Run

```sh
op run recipes/composition/pipeline/charon-pipeline-go-go
```
