# Composition-Pipeline-Cpp-Go

Stages the shared Go workers under `build/holons/` and runs a `cpp` orchestrator from a staged launcher.

## Run

```sh
op run recipes/composition/pipeline/charon-pipeline-cpp-go
```
