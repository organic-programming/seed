# Composition-Pipeline-Kotlin-Go

Stages the shared Go workers under `build/holons/` and runs a `kotlin` orchestrator from a staged launcher.

## Run

```sh
op run recipes/composition/pipeline/charon-pipeline-kotlin-go
```
