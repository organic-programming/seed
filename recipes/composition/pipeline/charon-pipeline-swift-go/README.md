# Composition-Pipeline-Swift-Go

Stages the shared Go workers under `build/holons/` and runs a `swift` orchestrator from a staged launcher.

## Run

```sh
op run recipes/composition/pipeline/charon-pipeline-swift-go
```
