# Composition-Direct-Csharp-Go

Stages the shared Go workers under `build/holons/` and runs a `csharp` orchestrator from a staged launcher.

## Run

```sh
op run recipes/composition/direct-call/charon-direct-csharp-go
```
