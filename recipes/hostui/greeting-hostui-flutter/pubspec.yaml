name: gudule
description: Godart greeting example — Flutter UI for the greeting daemon.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  holons:
    path: ../../../sdk/dart-holons
  grpc: ^5.1.0
  protobuf: ^6.0.0
  http2: ^2.3.0
  provider: ^6.1.2
  google_fonts: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
