schema: holon/v0
uuid: "8c4eb247-3b3c-4d45-b9d5-c38c518b4f89"
given_name: greeting
family_name: hostui-flutter
motto: Flutter host UI for the greeting daemon.
composer: Codex
clade: deterministic/pure
status: draft
born: "2026-03-11"
parents: []
reproduction: manual
generated_by: codex
description: |
  A standalone Flutter HostUI for the v0.4 greeting recipes. It uses the
  Dart SDK connect() path with GUDULE_DAEMON_TARGET when provided and a
  staged packaged daemon otherwise.
kind: composite
platforms: [macos]
build:
  runner: flutter
  defaults:
    target: macos
    mode: debug
requires:
  commands: [flutter, dart]
  files: [pubspec.yaml]
artifacts:
  primary: build/macos/Build/Products/Debug/Runner.app
