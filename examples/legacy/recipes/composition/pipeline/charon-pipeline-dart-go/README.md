# Composition-Pipeline-Dart-Go

Stages the shared Go workers under `build/holons/` and runs a `dart` orchestrator from a staged launcher.

## Run

```sh
op run recipes/composition/pipeline/charon-pipeline-dart-go
```
