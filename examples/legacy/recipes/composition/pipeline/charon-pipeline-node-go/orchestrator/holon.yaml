schema: holon/v0
given_name: charon
family_name: Composition-Orchestrator-Pipeline-Node-Go
kind: native
platforms: [macos]
build:
  runner: npm
requires:
  commands: [node, npm]
  files: [package.json, package-lock.json, src/main.js]
artifacts:
  binary: charon-pipeline-node-go-orchestrator
