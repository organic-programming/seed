# Composition-Fanout-Java-Go

Stages the shared Go workers under `build/holons/` and runs a `java` orchestrator from a staged launcher.

## Run

```sh
op run recipes/composition/fan-out/charon-fanout-java-go
```
