# Composition-Fanout-Rust-Go

Stages the shared Go workers under `build/holons/` and runs a `rust` orchestrator from a staged launcher.

## Run

```sh
op run recipes/composition/fan-out/charon-fanout-rust-go
```
