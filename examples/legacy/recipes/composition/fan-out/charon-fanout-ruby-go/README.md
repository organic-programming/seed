# Composition-Fanout-Ruby-Go

Stages the shared Go workers under `build/holons/` and runs a `ruby` orchestrator from a staged launcher.

## Run

```sh
op run recipes/composition/fan-out/charon-fanout-ruby-go
```
