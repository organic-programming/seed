# Composition-Fanout-Python-Go

Stages the shared Go workers under `build/holons/` and runs a `python` orchestrator from a staged launcher.

## Run

```sh
op run recipes/composition/fan-out/charon-fanout-python-go
```
