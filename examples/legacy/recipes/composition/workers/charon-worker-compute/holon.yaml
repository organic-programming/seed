schema: holon/v0
given_name: charon
family_name: Composition-Worker-Compute
kind: native
platforms: [macos]
build:
  runner: go-module
  main: ./cmd/worker
requires:
  commands: [go]
  files: [go.mod, cmd/worker/main.go, protos/compute/v1/compute.proto]
artifacts:
  binary: charon-worker-compute
