schema: holon/v0
given_name: charon
family_name: Composition-Worker-Transform
kind: native
platforms: [macos]
build:
  runner: go-module
  main: ./cmd/worker
requires:
  commands: [go]
  files: [go.mod, cmd/worker/main.go, protos/transform/v1/transform.proto]
artifacts:
  binary: charon-worker-transform
