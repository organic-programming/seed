# Composition-Direct-Node-Go

Stages the shared Go workers under `build/holons/` and runs a `node` orchestrator from a staged launcher.

## Run

```sh
op run recipes/composition/direct-call/charon-direct-node-go
```
