# ── Identity ──────────────────────────────────────────
schema: holon/v0
uuid: "f6f82422-f0fa-4d7e-a417-67602e749d09"
given_name: gudule
family_name: Greeting-Daemon-Rust
motto: Greets users in 56 languages with a Rust daemon.
composer: Codex
clade: deterministic/pure
status: draft
born: "2026-03-06"

# ── Lineage ───────────────────────────────────────────
parents: []
reproduction: manual
generated_by: manual

# ── Description ───────────────────────────────────────
description: |
  A Rust gRPC daemon that greets users in 56 languages. It serves the
  GreetingService contract over TCP and also accepts gRPC-Web requests so
  browser clients can reuse the same contract.

# ── Operational ───────────────────────────────────────
kind: native
build:
  runner: cargo
requires:
  commands: [cargo]
  files: [Cargo.toml]
artifacts:
  binary: gudule-daemon-greeting-rust
