# build.runner not yet implemented in op
schema: holon/v0
kind: native
build:
  runner: dart-package
requires:
  commands: [dart]
  files: [pubspec.yaml]
artifacts:
  binary: .op/build/bin/dart-hello-world
