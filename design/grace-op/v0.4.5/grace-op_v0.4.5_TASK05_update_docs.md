# TASK05 — Update Verification & Documentation

## Objective

Update `VERIFICATION_composition.md` and other relevant documents to
include the v0.4.5 assemblies (C++, C, Java as Daemons) and C composition recipes.

> [!CAUTION]
> **Do NOT modify or remove existing manual annotations** (❌ ⚠️ ✅ markers
> and `@bpds` date stamps). These are hand-written verification results.
> Only **append** new sections — never rewrite existing ones.

## Deliverables

1. **VERIFICATION_composition.md** — in the A.* existing sections, append the C++, C, Java daemons:
   - Part A.1 (Flutter): Add `# A.1.9 — Flutter + C++`, `# A.1.10 — Flutter + C`, `# A.1.11 — Flutter + Java`
   - Part A.2 (SwiftUI): Add `# A.2.9 — SwiftUI + C++`, etc.
   - Part A.3 (Kotlinui): Add `# A.3.9 — Kotlinui + C++`, etc.
   - Part A.4 (Web): Add `# A.4.9 — C++ + Web`, `# A.4.10 — C + Web`, etc.
   - Part A.5 (Dotnet): Add `# A.5.9 — Dotnet + C++`, etc.
   - Part A.6 (Qt): Add `# A.6.9 — Qt + C++`, etc.
   - Update composition B.* sections with C as orchestrator.
   - Update totals (66 assemblies, 36 compositions)

2. **Assembly Verification Checklist** — extend with C++/C/Java-specific items if needed.

3. **INDEX.md** — verify totals are consistent.

## Acceptance Criteria

- [ ] New sections appended without touching annotated lines
- [ ] Totals updated (66 assemblies, 36 compositions)
- [ ] All new `op build` / `op run` commands listed

