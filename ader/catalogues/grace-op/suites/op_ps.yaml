description: Suite validating op ps / op instances discovery of running holons.
defaults:
  profile: smoke
steps:
  ps-cli:
    check: op-ps-cli
    lane: progression
profiles:
  smoke:
    description: Verifies empty JSON output and one live observable holon in the sandbox registry.
    archive: never
    steps: [ps-cli]
  full:
    description: Same live `op ps` coverage as smoke for now.
    archive: never
    steps: [ps-cli]
