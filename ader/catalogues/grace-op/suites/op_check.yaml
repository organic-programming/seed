description: Suite validating op check with the CLI as the integration reference.
defaults:
  profile: smoke
steps:
  check-cli:
    check: op-check-cli
    lane: progression
profiles:
  smoke:
    description: CLI-first verification for the check surface
    archive: never
    steps:
    - check-cli
