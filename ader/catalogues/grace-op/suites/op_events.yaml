description: Suite validating op events streaming of lifecycle events.
defaults:
  profile: smoke
steps:
  events-cli:
    check: op-events-cli
    lane: progression
profiles:
  smoke:
    description: Verifies no-instance handling, help text, and JSON lifecycle events from a live observable holon.
    archive: never
    steps: [events-cli]
  full:
    description: Same live `op events --json` coverage as smoke for now.
    archive: never
    steps: [events-cli]
