description: Suite validating op metrics snapshot against a running holon.
defaults:
  profile: smoke
steps:
  metrics-cli:
    check: op-metrics-cli
    lane: progression
profiles:
  smoke:
    description: Verifies no-instance handling, help text, and JSON metrics from a live observable holon.
    archive: never
    steps: [metrics-cli]
  full:
    description: Same live `op metrics --json` coverage as smoke for now.
    archive: never
    steps: [metrics-cli]
