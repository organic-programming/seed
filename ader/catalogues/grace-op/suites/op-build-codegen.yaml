description: Suite validating build.codegen regeneration across migrated holons.
defaults:
  profile: smoke
steps:
  codegen-cli:
    check: op-build-codegen-cli
    lane: progression
profiles:
  smoke:
    description: Builds migrated holons, verifies runtime startup, generated-file restoration, and no-op rebuild stability.
    archive: never
    steps: [codegen-cli]
