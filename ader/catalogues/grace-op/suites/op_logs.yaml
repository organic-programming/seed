description: Suite validating op logs streaming from a running observability-enabled holon.
defaults:
  profile: smoke
steps:
  logs-cli:
    check: op-logs-cli
    lane: progression
profiles:
  smoke:
    description: Verifies no-instance handling, help text, and JSON log replay from a live observable holon.
    archive: never
    steps: [logs-cli]
  full:
    description: Same live `op logs --json` replay coverage as smoke for now.
    archive: never
    steps: [logs-cli]
