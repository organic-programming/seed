description: Suite validating op build with CLI-first integration and API/RPC alignment.
defaults:
  profile: smoke
steps:
  build-unit:
    check: op-build-unit
    lane: regression
  build-cli:
    check: op-build-cli
    lane: regression
  build-cli-composite:
    check: op-build-cli-composite
    lane: progression
  build-cli-composite-rebuild:
    check: op-build-cli-composite-rebuild
    lane: progression
  build-api:
    check: op-build-api
    lane: regression
  build-rpc:
    check: op-build-rpc
    lane: regression
profiles:
  smoke:
    description: Fast build verification for the CLI core plus API and RPC alignment.
    archive: never
    steps: [build-unit, build-cli, build-api, build-rpc]
  full:
    description: Exhaustive CLI-first build verification, including matrix and composite coverage.
    archive: never
    steps: [build-unit, build-cli, build-cli-composite, build-cli-composite-rebuild, build-api, build-rpc]
